#!/bin/sh
# Remove Tools → Waterfall menu entry before package files are removed.
. /usr/share/waterfall/tools-menu.sh 2>/dev/null || exit 0
remove_tools_menu
# Intentionally no uhttpd restart — see tools-menu.sh.
exit 0
